<%= simple_form_for @micropost, :html => {:class => 'form-vertical' } do |f| %>
  <!-- %= render 'shared/error_messages', :object => f.object % -->
  <%= f.input :content, :as => :text, :label => false, :input_html => { :cols => 50, :rows => 5 } %>
  <div class="actions">
    <%= f.submit "Submit", :class => 'btn btn-primary' %>
  </div>
<% end %>