<h1>Sign in</h1>

<%= simple_form_for(:session, :url => sessions_path, :html => {:class => 'form-horizontal' }) do |f| %>
  <%= f.input :email %>
  <%= f.input :password %>
  <div class="form-actions">
    <%= f.submit "Sign in", :class => 'btn btn-primary' %>
  </div> 
<% end %>
<p>New user? <%= link_to "Sign up now!", signup_path, :class => 'btn' %></p>